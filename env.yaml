API_ADZUNA:
 url: "${API_ADZUNA_url}"
 app_id:  "${API_ADZUNA_app_id}"
 app_key:  "${API_ADZUNA_app_key}"
 nbr_pages:  "${API_ADZUNA_nbr_pages}"
 results_per_page:  "${API_ADZUNA_results_per_page}"
 category:  "${API_ADZUNA_category}"
 what:  "${API_ADZUNA_what}"
 
jsonExport:
 output_folder:  "${OUTPUT_FOLDER}"
 
HelloWork:
 NETLOC:  "${NETLOC_HW}"

Global:
 USER_AGENT: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/116.0.0.0 Safari/537.36"